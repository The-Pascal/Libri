package com.example.libri.ui.theme

import androidx.compose.ui.graphics.Color

// Surface Hierarchy
val SurfaceBase = Color(0xFFFBF9F5)           // Primary canvas
val SurfaceContainerLowest = Color(0xFFFFFFFF)  // Visual "lift" card
val SurfaceContainerLow = Color(0xFFF5F3EF)     // Sectioning
val SurfaceContainer = Color(0xFFEFEEEA)        // Feed cards
val SurfaceContainerHighest = Color(0xFFE4E2DE) // Max visual lift
val SurfaceVariantBlur = Color(0xFFE4E2DE)      // Glassmorphic header

// Editorial Accents
val DeepTerracotta = Color(0xFF9A442D)          // Secondary/CTA
val TerracottaContainerSecondary = Color(0xFFE07A5F)
val TerracottaContainerTertiary = Color(0xFFFC9174)     // CTA Gradient End
val TerracottaFixed = Color(0xFFFFDBD2)         // Text highlight
val SageGreenContainer = Color(0xFF70A18A)      // Tertiary/Success
val DarkGreenContainer = Color(0xFF325544)

// Typography & Outlines
val DeepCharcoal = Color(0xFF1B1C1A)            // Primary Text (Never pure black)
val LightCharcoal = Color(0xFF444748)
val GhostOutline = Color(0xFFDBC1BA).copy(alpha = 0.15f) // Suggested border

val YellowColor = Color(0xFFD4AF37)

val DarkBlue = Color(0xFF0A1128)